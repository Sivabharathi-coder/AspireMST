const user = {
    name: "Siva",
    job: "Developer",
    company: "Aspire Systems"
};

const welcomeMessage = `Hi ${user.name},
Welcome to ${user.company}!
We are glad to have you onboard as a ${user.job}.
Let's build something great together.`;

console.log(welcomeMessage);

const project = "Job Portal";
const status = "Completed";
const hoursSpent = 6;

console.log(`Project: ${project}
Status: ${status}
Total Time Spent: ${hoursSpent} hours`);
